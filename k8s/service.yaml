apiVersion: v1
kind: Service
metadata:
  name: system-brain-erp
  namespace: system-brain-erp
  labels:
    app.kubernetes.io/name: system-brain-erp
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: system-brain-erp
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
